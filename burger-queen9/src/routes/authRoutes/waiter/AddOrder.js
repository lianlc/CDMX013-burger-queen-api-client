import { Link } from "react-router-dom"

export const WaiterNewOrderView = ()=>{
    return (
        <div>Confirmar orden
            <button><Link to="/">Agregar orden Waiter</Link></button>
        </div>
        
    )
}